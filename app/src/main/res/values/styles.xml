<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name = "AppTheme.NoActionBar">
        <item name="android:windowTranslucentStatus">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="mediumCardView">
        <item name="cardElevation">2dp</item>
        <item name="cardCornerRadius">4dp</item>
        <item name="cardBackgroundColor">@color/mediumComponent</item>
        <item name="cardUseCompatPadding">true</item>
    </style>

    <style name="loginButton" parent="Theme.AppCompat.Light">
        <item name="rippleColor">@color/colorButtonPress</item>
        <item name="backgroundTint">@color/colorButtonNormal</item>
        <item name="android:textColor">@color/colorText</item>
    </style>

    <style name="tabLayoutTextAppearance" parent="TextAppearance.AppCompat.Widget.ActionBar.Title.Inverse">
        <item name="android:textSize">25sp</item>
        <item name="android:textAllCaps">false</item>
    </style>

</resources>
